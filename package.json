{
  "name": "lrlex-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LR LEX — sito statico + generatore editoriale /insights (SSG)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:insights": "node scripts/build-insights.mjs",
    "build:practices": "node scripts/build-practices.mjs",
    "build:sitemap": "node scripts/build-sitemap.mjs",
    "check:content": "node scripts/check-content.mjs",
    "check:blocklist": "node scripts/check-blocklist.mjs",
    "check:links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0"
  }
}
